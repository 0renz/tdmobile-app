class Contato {
  int id;
  String nome;
  String telefone;
  String email;
  String relacao;
  String endereco;

  Contato (this.id, this.nome, this.telefone, this.email, this.relacao, this.endereco);
}